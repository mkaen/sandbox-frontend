VITE_DEV_SERVER_PORT=8080

VITE_API_HOST=127.0.0.1
VITE_API_PORT=8000

# Cloudflare R2 Worker (non-prod)
VITE_R2_WORKER_URL=https://cloudflare-r2-worker.your-app.workers.dev
# Visible in the browser — non-prod only; same value as wrangler UPLOAD_KEY secret
VITE_R2_UPLOAD_KEY=
